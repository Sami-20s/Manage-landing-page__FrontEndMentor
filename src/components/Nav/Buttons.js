import React from "react";

const Buttons = () => {
    return (
        <div className="nav__wrapper__buttons hide-for-mobile">
            <button className="btn">Get Started</button>
        </div>
    );
};

export default Buttons;
