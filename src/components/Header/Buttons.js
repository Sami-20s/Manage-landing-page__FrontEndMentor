import React from "react";

const Buttons = () => {
    return (
        <div className="header__wrapper__text__buttons">
            <button className="btn">Get Started</button>
        </div>
    );
};

export default Buttons;
